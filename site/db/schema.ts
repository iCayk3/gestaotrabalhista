import { sqliteTable,text,integer,primaryKey,index } from 'drizzle-orm/sqlite-core';
export const companies=sqliteTable('companies',{id:text('id').primaryKey(),state:text('state').notNull(),version:integer('version').notNull(),op:text('op').notNull()});
export const members=sqliteTable('members',{company:text('company').notNull().references(()=>companies.id),user:text('user').notNull(),email:text('email').notNull(),role:text('role').notNull()},t=>[primaryKey({columns:[t.company,t.user]}),index('idx_members_user').on(t.user)]);
export const history=sqliteTable('history',{id:text('id').primaryKey(),company:text('company').notNull().references(()=>companies.id),version:integer('version').notNull(),actor:text('actor').notNull(),reason:text('reason').notNull(),created:text('created').notNull(),snapshot:text('snapshot').notNull()},t=>[index('idx_history_company_version').on(t.company,t.version)]);
export const invites=sqliteTable('invites',{hash:text('hash').primaryKey(),company:text('company').notNull().references(()=>companies.id),expires:text('expires').notNull(),used:text('used'),recipient:text('recipient').notNull()},t=>[index('idx_invites_company').on(t.company)]);

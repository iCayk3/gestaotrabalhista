export function authPage(mode,error=''){
 const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const register=mode==='register',change=mode==='password';const title=change?'Alterar senha':register?'Criar acesso por convite':'Entrar';
 return `<!doctype html><html lang="pt-BR"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${title} · SOL</title><style>body{background:#edf3f4;color:#18363e;font:16px Arial;margin:0;display:grid;min-height:100vh;place-items:center}main{width:min(380px,85vw);background:white;padding:32px;border-radius:12px}label{display:block;margin:20px 0 8px}input{box-sizing:border-box;width:100%;padding:12px;border:1px solid #acbfc4;border-radius:6px}button{width:100%;margin-top:24px;padding:13px;background:#234c54;color:white;border:0;border-radius:7px;font:inherit}a{color:#234c54}p{line-height:1.5}.error{color:#a32823}</style><main><h1>${title}</h1><p>Acesso individual ao controle trabalhista.</p>${error?`<p role="alert" class="error">${esc(error)}</p>`:''}<form method="post" action="/auth/${mode}">${!change?'<label for="email">E-mail</label><input id="email" name="email" type="email" required maxlength="200" autocomplete="username">':'<label for="current">Senha atual</label><input id="current" name="current" type="password" required maxlength="128" autocomplete="current-password">'}${register?'<label for="invite">Código do convite</label><input id="invite" name="invite" required maxlength="100" autocomplete="off">':''}<label for="password">${change?'Nova senha':'Senha'}</label><input id="password" name="password" type="password" required ${register||change?'minlength="12"':''} maxlength="128" autocomplete="${register||change?'new-password':'current-password'}"><button>${title}</button></form><p><a href="${change?'/':register?'/auth/login':'/auth/register'}">${change?'Voltar':register?'Já tenho acesso':'Recebi um convite'}</a></p>${!register&&!change?'<p>Esqueceu a senha? Solicite a recuperação ao administrador do servidor.</p>':''}</main></html>`;
}
